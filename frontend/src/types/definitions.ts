export type Field = {
  id: string;
  name: string;
  description: string;
  image: string;
};

export type Program = {
  id: string;
  name: string;
  description: string;
  type: string;
  priorEducation: string;
  additionalInformation: string;
};

export type Subject = {
  id: string;
  name: string;
  description: string;
};
